// Sumar una variable de 1 en 1 e imprimir en la consola

// bucle for

for (let i = 0; i < 5; i++) {
    suma = suma + i;
    console.log('Variable de iteración: ', i);
 }
 console.log('Variable suma: ', suma);

// bucle white

while(suma < 3) {
    suma = suma + 1;
        console.log(suma);
    };